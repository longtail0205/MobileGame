// =====================================================================
// ゲーム全体の設定
// 数値を書き換えるだけでバランス調整ができます。
// =====================================================================
window.GameData = window.GameData || {};

GameData.config = {
  title: 'ガチャモン',
  subtitle: 'GACHA MONSTERS',
  version: '1.0.0',

  // セーブデータの保存キー（変えると別データ扱いになる）
  saveKey: 'gachamon_save_v1',
  // データエディタの一時上書きデータの保存キー
  overrideKey: 'gachamon_data_override_v1',

  // true にすると「その他」タブにデバッグ機能（ポイント付与など）が表示される
  debug: true,

  // ---- 経済 -----------------------------------------------------------
  startPoints: 0,        // 開始時の所持ポイント
  freeGachaPulls: 3,     // 開始時の無料ガチャ回数
  loginBonus: 300,       // 1日1回のログインボーナス
  rewards: {
    wildBase: 10,        // 野生勝利ポイント = (wildBase + wildPerLevel × Lv) × レア度倍率
    wildPerLevel: 3,
    trainerDefault: 150, // トレーナーに reward が無い場合の勝利ポイント
  },
  blackoutPointLoss: 0,  // 全滅時に失うポイントの割合（0〜1）

  // ---- モンスター ------------------------------------------------------
  partyMax: 6,
  maxLevel: 100,
  iv: 20,                // 個体値（全員共通）
  expMultiplier: 1.0,    // 獲得経験値の倍率
  expShareRatio: 0.5,    // 戦闘に出ていないパーティメンバーが得る経験値の割合（0で無効）

  // ---- レベル上限（バッジ数で解放） -----------------------------------
  levelCaps: [20, 30, 35, 40, 50],   // [バッジ0, 1, 2, 3, 4個] のときの上限Lv
  catchUpExp: [                       // 上限との差が大きいほど経験値ボーナス（上から順に判定）
    { below: 15, mult: 3 },           // 上限-15 以下 → ×3
    { below: 10, mult: 2 },           // 上限-10 以下 → ×2
    { below: 5, mult: 1.5 },          // 上限-5  以下 → ×1.5
  ],
  overflowExpPerPoint: 50,            // 上限に達したモンスターのあふれた経験値 50 につき 1pt
  overflowDailyMax: 300,              // あふれ経験値から得られるポイントの1日の上限

  // ---- バトル ----------------------------------------------------------
  critChance: 1 / 16,
  critMultiplier: 1.5,
  stab: 1.5,             // タイプ一致ボーナス

  // ---- フィールド ------------------------------------------------------
  encounterRate: 0.12,   // 草むら1歩あたりのエンカウント率（マップ側で上書き可）
  walkMs: 220,           // 1マス歩く時間
  runMs: 120,            // 1マス走る時間（Bボタン押しながら）
  textSpeed: { slow: 55, normal: 30, fast: 8 },  // 1文字あたりのms

  // ---- 主人公 ----------------------------------------------------------
  player: {
    defaultName: 'ユウ',
    look: { skin: '#f8d0a8', hair: '#4a3020', hairStyle: 'short', shirt: '#e04040', pants: '#304880', hat: '#e04040', accent: '#ffffff' },
  },
};
