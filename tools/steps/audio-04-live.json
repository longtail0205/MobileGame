[
  { "goto": "tools/audio-preview.html" },
  { "waitFor": "window.App && App.audio && window.AudioPreview", "timeout": 5000 },
  { "eval": "App.audio.playBgm('town'); App.audio.play('select'); App.audio.play('nope'); App.audio.playBgm('nope'); JSON.stringify(App.audio.status())" },
  { "eval": "App.audio.currentBgm() === 'town' && App.audio.status().ctxState === null ? 'PASS reserved before unlock' : 'FAIL'" },
  { "key": "KeyZ" },
  { "wait": 400 },
  { "eval": "(function(){ var s = App.audio.status(); return (s.unlocked && s.ctxState === 'running' && s.playing === 'town' && s.timer ? 'PASS unlocked by keydown, reserved BGM started ' : 'FAIL ') + JSON.stringify(s); })()" },
  { "eval": "window.__t0 = App.audio.status().startedAt; App.audio.playBgm('town'); App.audio.playBgm('town'); App.audio.status().startedAt === window.__t0 ? 'PASS same BGM not restarted' : 'FAIL restarted'" },
  { "eval": "App.audio.playBgm('town', { restart: true }); App.audio.status().startedAt !== window.__t0 ? 'PASS restart option' : 'FAIL'" },
  { "eval": "App.audio.playBgm('battleWild'); (function(){ var s = App.audio.status(); return (s.playing === 'battleWild' && s.fading >= 1 ? 'PASS crossfade started ' : 'FAIL ') + JSON.stringify(s); })()" },
  { "wait": 800 },
  { "eval": "(function(){ var s = App.audio.status(); return (s.fading === 0 && s.time > 0.5 ? 'PASS fade finished, clock running ' : 'FAIL ') + JSON.stringify(s); })()" },
  { "eval": "(async function(){ var n = App.audio.list(); for (var i = 0; i < n.sfx.length; i++) { App.audio.play(n.sfx[i]); await new Promise(function(r){ setTimeout(r, 60); }); } return 'played ' + n.sfx.length + ' sfx'; })()" },
  { "eval": "(async function(){ var n = App.audio.list(); for (var i = 0; i < n.bgm.length; i++) { App.audio.playBgm(n.bgm[i]); await new Promise(function(r){ setTimeout(r, 350); }); } return 'cycled ' + n.bgm.length + ' bgm: ' + JSON.stringify(App.audio.status()); })()" },
  { "wait": 1500 },
  { "eval": "App.audio.stopBgm(); JSON.stringify(App.audio.status())" },
  { "wait": 1000 },
  { "eval": "(function(){ var s = App.audio.status(); return (s.playing === null && s.bgm === null && s.fading === 0 && !s.timer ? 'PASS stopped, timer cleared ' : 'FAIL ') + JSON.stringify(s); })()" },
  { "eval": "window.__st = { data: { settings: { sound: true, volume: 0.6 } }, setting: function(k){ return this.data.settings[k]; }, setSetting: function(k, v){ this.data.settings[k] = v; App.events.emit('settings:changed', { key: k, value: v }); } }; App.state = window.__st; App.state.setSetting('sound', false); App.audio.isMuted() === true ? 'PASS settings:changed sound=false -> muted' : 'FAIL'" },
  { "eval": "App.state.setSetting('volume', 0.25); App.audio.getVolume() === 0.25 ? 'PASS settings:changed volume' : 'FAIL ' + App.audio.getVolume()" },
  { "eval": "App.audio.play('hit'); App.audio.setMuted(false); (App.state.data.settings.sound === true && !App.audio.isMuted()) ? 'PASS setMuted -> state.setSetting' : 'FAIL'" },
  { "eval": "App.audio.setVolume(0.8); App.state.data.settings.volume === 0.8 ? 'PASS setVolume -> state' : 'FAIL'" },
  { "eval": "App.audio.setVolume('abc'); App.audio.setVolume(5); App.audio.getVolume() === 1 ? 'PASS volume clamp' : 'FAIL ' + App.audio.getVolume()" },
  { "eval": "App.events.emit('state:loaded', {}); App.audio.getVolume()" },
  { "eval": "App.audio.playBgm('victory'); App.audio.play('levelup'); App.audio.play('win'); JSON.stringify(App.audio.status())" },
  { "wait": 1200 },
  { "eval": "App.audio.playBgm(null); App.audio.playBgm(''); App.audio.play(null); App.audio.play(undefined); App.audio.stopBgm(); App.audio.stopBgm(); 'PASS null/empty args ignored'" },
  { "shot": "04-live" }
]
