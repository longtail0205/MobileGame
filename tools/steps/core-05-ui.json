[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=settings" },
  { "waitFor": "window.App && App.ready", "timeout": 8000 },
  { "wait": 300 },
  { "shot": "30-settings-top" },
  { "eval": "window.scrollTo(0, 900); 'scrolled'" },
  { "wait": 200 },
  { "shot": "31-settings-bottom" },
  { "eval": "window.scrollTo(0, 0); 'top'" },
  { "eval": "Array.from(document.querySelectorAll('.settings-section .btn')).find(b => b.textContent.includes('+1000')).click(); 'clicked'" },
  { "wait": 250 },
  { "shot": "32-points-pop" },
  { "wait": 900 },
  { "eval": "document.querySelector('#tb-points .pt-value').textContent + ' / state=' + App.state.points()" },
  { "eval": "App.ui.toast('インフォメーション'); App.ui.toast('ポイントを てにいれた！', {type:'success'}); App.ui.toast('ポイントが たりません', {type:'warn'}); App.ui.toast('エラーが おきました', {type:'error'}); 'toasts'" },
  { "wait": 350 },
  { "shot": "33-toasts" },
  { "wait": 2500 },
  { "eval": "(function(){ const el = App.util.el; const U = App.ui; const mons = App.data.monsters(); const byR = {}; mons.forEach(m => { if (!byR[m.rarity]) byR[m.rarity] = m; }); const order = App.data.rarityOrder(); const cards = order.filter(r => byR[r]).map((r, i) => { const inst = App.monster.create(byR[r].id, 5 + i * 10, { limitBreak: i }); inst.hp = Math.floor(App.monster.stats(inst).hp * [1, 0.6, 0.35, 0.1, 1][i]); return U.monsterCard(inst, { size: 'm', showHp: true, badge: i === 0 ? 'NEW' : null, selected: i === 1, onClick: () => {} }); }); const extra = [U.monsterCard(mons[0].id, { size: 'm', silhouette: true }), U.monsterCard(mons[1].id, { size: 'm', unknown: true })]; const smalls = order.filter(r => byR[r]).map(r => U.monsterCard(byR[r].id, { size: 's' })); const big = byR.UR ? U.monsterCard(App.monster.create(byR.UR.id, 50, {limitBreak: 5}), { size: 'l', showHp: true }) : null; const badges = el('div', { class: 'row', style: 'flex-wrap:wrap;margin:10px 0' }, order.map(r => U.rarityBadge(r)), App.data.types().slice(0, 9).map(t => U.typeBadge(t)), U.stars(3), U.stars(5)); const hp = el('div', { style: 'display:grid;gap:6px;width:200px' }, U.hpBar(90, 100), U.hpBar(40, 100), U.hpBar(10, 100)); const body = el('div', {}, el('div', { class: 'row', style: 'flex-wrap:wrap;gap:14px;align-items:flex-start' }, cards, extra), el('div', { class: 'row', style: 'gap:10px;margin-top:14px;align-items:flex-end' }, smalls, big), badges, hp, el('div', { class: 'btn-row' }, el('button', { class: 'btn', text: 'ふつう' }), el('button', { class: 'btn btn-primary', text: 'primary' }), el('button', { class: 'btn btn-gold', text: 'gold' }), el('button', { class: 'btn btn-danger', text: 'danger' }), el('button', { class: 'btn', disabled: true, text: 'disabled' }), el('button', { class: 'btn btn-sm btn-primary', text: 'small' }))); U.modal({ title: 'UIサンプル', body, size: 'wide', buttons: [{ label: 'とじる', value: 'x', primary: true }] }).then(v => window.__m = v); return Object.keys(byR).join(','); })()" },
  { "wait": 500 },
  { "shot": "34-ui-showcase" },
  { "key": "Escape" },
  { "wait": 300 },
  { "eval": "'modal result=' + window.__m + ' open=' + App.ui.isModalOpen()" },
  { "eval": "App.ui.confirm('ほんとうに よろしいですか？', {danger: true, okLabel: 'けす'}).then(v => window.__c = v); 'confirm'" },
  { "wait": 400 },
  { "shot": "35-confirm" },
  { "click": ".modal-footer .btn-danger" },
  { "wait": 300 },
  { "eval": "App.ui.prompt('ニックネームを いれてね', {title: 'ニックネーム', value: 'ポチ', maxLength: 6}).then(v => window.__p = v); 'prompt'" },
  { "wait": 400 },
  { "eval": "document.querySelector('.modal input').value = '  タマ '; 'typed'" },
  { "key": "Enter" },
  { "wait": 300 },
  { "eval": "JSON.stringify({confirm: window.__c, prompt: window.__p, open: App.ui.isModalOpen()})" },
  { "eval": "Array.from(document.querySelectorAll('.settings-section .btn')).map(b => b.textContent).join(' | ')" },
  { "eval": "(function(){ const btns = Array.from(document.querySelectorAll('.settings-section .btn')); const f = t => btns.find(b => b.textContent.includes(t)); f('全モンスター入手').click(); f('パーティ Lv+5').click(); f('無料ガチャ +3').click(); return JSON.stringify({owned: App.state.ownedCount(), total: App.data.monsters().length, lv: App.state.party().map(i => i.level), free: App.state.freePulls()}); })()" },
  { "eval": "(function(){ const btns = Array.from(document.querySelectorAll('.settings-section .btn')); btns.find(b => b.textContent.includes('データ検証レポート')).click(); return 'report'; })()" },
  { "wait": 400 },
  { "shot": "36-validation-report" },
  { "key": "Escape" },
  { "wait": 300 },
  { "shot": "37-settings-after" }
]
