[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=adventure" },
  { "waitFor": "window.App && App.ready", "timeout": 15000 },
  { "eval": "new Promise(function(r){var s=document.createElement('script');s.src='tools/battle-test.js';s.onload=function(){r('loaded')};document.body.appendChild(s);})" },
  { "eval": "App.state.addMonster('hinokon', {level: 14}); App.state.addMonster('mizupyon', {level: 9}); var h=App.state.owned('hinokon'), m=App.state.owned('mizupyon'); h.exp = App.monster.expForLevel(15,'medium_fast') - 2; m.exp = App.monster.expForLevel(10,'medium_fast') - 2; JSON.stringify([h.moves.map(function(x){return x.id}), m.moves.map(function(x){return x.id})])" },
  { "eval": "window.__r = null; App.battle.startWild('happamaru', 7).then(function(r){ window.__r = r; }); 'started'" },
  { "eval": "BattleTest.autoplay({ stopAt: 'command' }).then(function(){ return App.battle.debugState().phase; })" },
  { "click": ".bt-cmd-item[data-cmd='fight']" },
  { "wait": 150 },
  { "eval": "App.battle.debugState().phase" },
  { "click": ".bt-move[data-index='2']" },
  { "wait": 250 },
  { "shot": "k01-move-250" },
  { "wait": 300 },
  { "shot": "k02-move-550" },
  { "wait": 350 },
  { "shot": "k03-move-900" },
  { "eval": "window.__ap = null; BattleTest.autoplay({ move: 2 }).then(function(x){ window.__ap = x; }); 'go'" },
  { "waitFor": "window.__r !== null && window.__ap !== null", "timeout": 60000 },
  { "eval": "window.__ap.log.join(' / ').split(String.fromCharCode(10)).join('')" },
  { "eval": "var h=App.state.owned('hinokon'), m=App.state.owned('mizupyon'); JSON.stringify({ result: window.__r, h: [h.level, h.moves.map(function(x){return x.id})], m: [m.level, m.moves.map(function(x){return x.id})] })" }
]
