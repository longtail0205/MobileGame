[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=gacha" },
  { "waitFor": "window.App && App.ready", "timeout": 8000 },
  { "eval": "App.state.addMonster('hinokon', {level: 18}); var r=App.state.evolve('hinokon'); GameData.gacha.banners.forEach(function(b){ b.pool=['hinokon']; b.pickup=[]; }); App.state.addPoints(5000,'debug'); JSON.stringify({ev: r && r.to, owned: Object.keys(App.state.data.collection)})" },
  { "eval": "var x=App.gacha.pull(GameData.gacha.banners[0].id, 1, {}); JSON.stringify(x.map(function(y){return [y.speciesId,y.ownedId,y.isNew,y.limitBreak,y.refund]}))" },
  { "eval": "App.main.switchTab('dex'); App.main.switchTab('gacha'); true" },
  { "wait": 300 },
  { "click": ".gc-pull-free" },
  { "wait": 300 },
  { "click": ".gc-skip" },
  { "waitFor": "App.gachaTab._show._director() && App.gachaTab._show._director().phase === 'end'", "timeout": 9000 },
  { "wait": 700 },
  { "shot": "g1-family-lb" },
  { "eval": "(document.querySelector('.gc-rv-status')||{}).textContent" },
  { "eval": "JSON.stringify({lb: App.state.owned('hinojishi').limitBreak, owned: Object.keys(App.state.data.collection)})" },
  { "click": ".gc-close" },
  { "waitFor": "!document.querySelector('.gc-ov')", "timeout": 3000 },
  { "eval": "App.main.switchTab('dex'); App.dexTab.open('hinokon'); true" },
  { "wait": 500 },
  { "eval": "document.querySelector('.dx-sec-evo').scrollIntoView({block:'center'}); document.querySelector('.dx-sec-evo').textContent + ' | gacha: ' + document.querySelector('.dx-sec-gacha').textContent" },
  { "wait": 200 },
  { "shot": "g2-dex-evo" }
]
