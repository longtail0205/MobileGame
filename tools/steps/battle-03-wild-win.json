[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=adventure" },
  { "waitFor": "window.App && App.ready", "timeout": 15000 },
  { "eval": "new Promise(function(r){var s=document.createElement('script');s.src='tools/battle-test.js';s.onload=function(){r('loaded')};document.body.appendChild(s);})" },
  { "eval": "App.state.addMonster('hinokon', {level: 12}); App.state.addMonster('mizupyon', {level: 8}); var h=App.state.owned('hinokon'); h.exp = App.monster.expForLevel(13,'medium_fast') - 3; window.__before = { points: App.state.points(), hLv: h.level, mExp: App.state.owned('mizupyon').exp, emberPP: h.moves[2].pp, wins: App.state.data.stats.wins, battles: App.state.data.stats.battles }; window.__ev=[]; ['battle:start','battle:end','monster:updated'].forEach(function(n){App.events.on(n,function(p){window.__ev.push(n+':'+JSON.stringify(p))})}); JSON.stringify(window.__before)" },
  { "eval": "window.__r = null; App.battle.startWild('happamaru', 5).then(function(r){ window.__r = r; }); [App.battle.isActive(), App.main.isTabLocked()].join()" },
  { "eval": "BattleTest.autoplay({ move: 2, stopAt: 'levelup' }).then(function(x){ return JSON.stringify(x.stopped) + ' ' + x.log.join(' / '); })", "timeout": 60000 },
  { "wait": 300 },
  { "shot": "w10-levelup1" },
  { "key": "KeyZ" },
  { "wait": 200 },
  { "shot": "w11-levelup2" },
  { "eval": "BattleTest.autoplay({ move: 2, stopAt: 'wait' }).then(function(x){ return App.battle.debugState().text; })", "timeout": 60000 },
  { "wait": 200 },
  { "shot": "w12-after-level" },
  { "eval": "BattleTest.autoplay({ move: 2 }).then(function(x){ return x.log.join(' / '); })", "timeout": 60000 },
  { "waitFor": "window.__r !== null", "timeout": 10000 },
  { "eval": "var h=App.state.owned('hinokon'), m=App.state.owned('mizupyon'); JSON.stringify({ result: window.__r, pointsDelta: App.state.points() - __before.points, hLv: h.level, mExpDelta: m.exp - __before.mExp, emberPP: h.moves[2].pp + '/' + __before.emberPP, stats: App.state.data.stats, seen: App.state.isSeen('happamaru'), layerHidden: document.getElementById('battle-layer').hidden, locked: App.main.isTabLocked(), active: App.battle.isActive(), ev: window.__ev })" },
  { "shot": "w13-end" }
]
