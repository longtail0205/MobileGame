[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=settings" },
  { "waitFor": "window.App && App.ready", "timeout": 8000 },
  { "eval": "(function(){ const mv = Object.keys(GameData.moves).slice(0,5); GameData.monsters.push({ id:'zz_test', no: 99999, name:'テストモン', rarity:'N', types:[Object.keys(GameData.types)[0]], baseStats:{hp:45,atk:55,def:40,spa:60,spd:45,spe:60}, learnset:[{lv:1,move:mv[0]},{lv:7,move:mv[1]},{lv:9,move:mv[2]},{lv:10,move:mv[3]},{lv:10,move:mv[4]}], expGroup:'medium_fast', baseExp:60 }); window.__mv = mv; return 'injected ' + mv.join(','); })()" },
  { "eval": "(function(){ const M = App.monster; const r = []; const ok = (name, got, exp) => r.push((JSON.stringify(got) === JSON.stringify(exp) ? 'OK  ' : 'NG  ') + name + ' got=' + JSON.stringify(got) + ' exp=' + JSON.stringify(exp)); const a = M.create('zz_test', 5); ok('stats L5', M.stats(a), {hp:20,atk:11,def:10,spa:12,spd:10,spe:12}); ok('hp L5 = max', a.hp, 20); ok('moves L5', a.moves.map(m=>m.id), [__mv[0]]); ok('exp L5', a.exp, 125); const b = M.create('zz_test', 50); ok('stats L50', M.stats(b), {hp:115,atk:70,def:55,spa:75,spd:60,spe:75}); b.limitBreak = 5; ok('stats L50 +5', M.stats(b), {hp:138,atk:84,def:66,spa:90,spd:72,spe:90}); ok('expForLevel mf', [1,2,5,10,100].map(l=>M.expForLevel(l,'medium_fast')), [0,8,125,1000,1000000]); ok('expForLevel fast', [1,5,10,100].map(l=>M.expForLevel(l,'fast')), [0,100,800,800000]); ok('expForLevel ms', [1,2,5,100].map(l=>M.expForLevel(l,'medium_slow')), [0,9,135,1059860]); ok('expForLevel slow', [1,5,100].map(l=>M.expForLevel(l,'slow')), [0,156,1250000]); ok('expForLevel default', M.expForLevel(10), 1000); const c = M.create('zz_test', 5); c.hp = 10; const res = M.addExp(c, 875); ok('addExp level', [res.oldLevel,res.newLevel,res.levelsGained], [5,10,5]); ok('addExp learned', res.learned, [__mv[1],__mv[2],__mv[3]]); ok('addExp pending', res.pending, [__mv[4]]); ok('addExp hp +11', c.hp, 21); ok('statsAfter.hp', res.statsAfter.hp, 31); ok('exp=1000', c.exp, 1000); ok('expProgress 0', M.expProgress(c), 0); ok('expToNext', M.expToNext(c), 1331-1000); const d = M.create('zz_test', 5); M.addExp(d, 30); ok('expProgress 30/91', Math.round(M.expProgress(d)*1000)/1000, Math.round(30/91*1000)/1000); const f = M.create('zz_test', 5); f.hp = 0; M.addExp(f, 875); ok('fainted hp stays 0', f.hp, 0); const mx = M.create('zz_test', 99); M.addExp(mx, 99999999); ok('maxLevel cap', [mx.level, mx.exp], [100, 1000000]); ok('learnable L10', M.learnableMoves(c), __mv.slice(0,5)); ok('setMoves ok', M.setMoves(c, [__mv[4], __mv[0]]), true); ok('setMoves result', c.moves.map(m=>m.id), [__mv[4], __mv[0]]); ok('setMoves 5 NG', M.setMoves(c, __mv.slice(0,5)), false); ok('setMoves 0 NG', M.setMoves(c, []), false); ok('setMoves unlearned NG', M.setMoves(a, [__mv[3]]), false); c.moves[0].pp = 1; c.status='burn'; c.hp = 3; M.heal(c); ok('heal', [c.hp, c.status, c.moves[0].pp === M.maxPP(c.moves[0].id)], [31, null, true]); ok('expYield wild L10', M.expYield(c, false), 85); ok('expYield trainer L10', M.expYield(c, true), 128); ok('wildPoints L10 N x' + GameData.rarities.N.pointMult, M.wildPoints(c), Math.round(40 * GameData.rarities.N.pointMult)); ok('displayName', M.displayName(c), 'テストモン'); c.nickname='ポチ'; ok('displayName nick', M.displayName(c), 'ポチ'); ok('create opts.moves', M.create('zz_test', 10, {moves:[__mv[2], 'nope']}).moves.map(m=>m.id), [__mv[2]]); let fl = 0; for (let L = 1; L <= 100; L++) { for (let lb = 0; lb <= 5; lb++) { const t = M.create('zz_test', L); t.limitBreak = lb; const s = M.stats(t); const base = M.stats(Object.assign({}, t, {limitBreak: 0})); ['hp','atk','def','spa','spd','spe'].forEach(k => { const exp = Math.floor(base[k] * (100 + 4 * lb) / 100); if (s[k] !== exp) fl++; }); } } ok('limitBreak integer-exact (600 cases)', fl, 0); return r.join(String.fromCharCode(10)); })()" },
  { "eval": "(function(){ const S = App.state; const r = []; const ok = (name, got, exp) => r.push((JSON.stringify(got) === JSON.stringify(exp) ? 'OK  ' : 'NG  ') + name + ' got=' + JSON.stringify(got) + ' exp=' + JSON.stringify(exp)); const ev = []; ['points:changed','collection:changed','party:changed','freepulls:changed','dex:changed'].forEach(n => App.events.on(n, p => ev.push(n))); ok('start points', S.points(), 0); ok('addPoints', S.addPoints(500, 'test'), 500); ok('spend 300', S.spendPoints(300, 'test'), true); ok('points 200', S.points(), 200); ok('spend 1000 NG', S.spendPoints(1000), false); ok('points still 200', S.points(), 200); ok('stats earned/spent', [S.data.stats.pointsEarned, S.data.stats.pointsSpent], [500, 300]); ok('addPoints negative', S.addPoints(-50, 'loss'), 150); S.addPoints(50); ok('freePulls', S.freePulls(), 3); ok('useFreePull', S.useFreePull(), true); ok('freePulls 2', S.freePulls(), 2); const a1 = S.addMonster('zz_test'); ok('add new', [a1.isNew, a1.limitBreak, a1.refund, a1.joinedParty, a1.inst.level], [true, 0, 0, true, GameData.gacha.startLevel]); ok('dex owned', [S.isOwned('zz_test'), S.isSeen('zz_test'), !!S.data.dex.owned.zz_test], [true,true,true]); const hp0 = S.owned('zz_test').hp; const a2 = S.addMonster('zz_test'); ok('dup -> LB1', [a2.isNew, a2.limitBreak, a2.refund], [false, 1, 0]); ok('LB raises hp', S.owned('zz_test').hp >= hp0, true); for (let i = 0; i < 10; i++) { const x = S.addMonster('zz_test'); if (x.limitBreak >= GameData.gacha.maxLimitBreak) break; } ok('max LB', S.owned('zz_test').limitBreak, GameData.gacha.maxLimitBreak); const p0 = S.points(); const a3 = S.addMonster('zz_test'); ok('max LB -> refund', [a3.refund, S.points() - p0], [GameData.rarities.N.refund, GameData.rarities.N.refund]); const others = App.data.monsters().filter(m => m.id !== 'zz_test').slice(0, 7).map(m => m.id); others.forEach(id => S.addMonster(id)); ok('party max', S.partyIds().length, App.data.config.partyMax); ok('owned count', S.ownedCount(), 8); ok('ownedList order first', S.ownedList()[0].speciesId, 'zz_test'); const before = S.partyIds(); ok('moveInParty', S.moveInParty(0, 2), true); ok('moved', S.partyIds()[2], before[0]); ok('removeFromParty', S.removeFromParty(before[1]), true); ok('party 5', S.partyIds().length, 5); ok('addToParty', S.addToParty(before[1]), true); ok('addToParty full NG', S.addToParty(others[6]), false); ok('setParty([]) NG', S.setParty([]), false); ok('setParty dedupe', S.setParty(['zz_test', others[0], 'zz_test', 'nope']), true); ok('party after set', S.partyIds(), ['zz_test', others[0]]); ok('remove -> 1', S.removeFromParty(others[0]), true); ok('remove last NG', S.removeFromParty('zz_test'), false); S.owned('zz_test').hp = 0; ok('hasHealthy false', S.hasHealthy(), false); S.healAll(); ok('healAll', S.hasHealthy(), true); ok('firstHealthy', S.firstHealthy().speciesId, 'zz_test'); S.markSeen(others[0]); ok('markSeen', S.isSeen(others[0]), true); S.setFlag('foo', 1); ok('flag', S.flag('foo'), 1); const tid = Object.keys(GameData.trainers)[0]; ok('trainer not defeated', S.isTrainerDefeated(tid), false); S.setTrainerDefeated(tid); ok('trainer defeated', S.isTrainerDefeated(tid), true); S.data.trainers[tid] = '2000-01-01'; ok('daily rematch (' + GameData.trainers[tid].rematch + ')', S.isTrainerDefeated(tid), GameData.trainers[tid].rematch !== 'daily'); S.takePickup('p1'); ok('pickup', S.isPickupTaken('p1'), true); S.setSetting('textSpeed', 'fast'); ok('setting', S.setting('textSpeed'), 'fast'); S.recordGacha([{speciesId:'zz_test', rarity:'N', bannerId:'standard', isNew:false, limitBreak:1, refund:0}]); ok('recordGacha', [S.data.gacha.history.length, S.data.gacha.totalPulls], [1, 1]); S.incStat('steps', 5); S.incStat('battles'); ok('incStat', [S.data.stats.steps, S.data.stats.battles], [5, 1]); ok('events fired', ['points:changed','collection:changed','party:changed','freepulls:changed','dex:changed'].every(n => ev.includes(n)), true); S.saveNow(); ok('saved to localStorage', JSON.parse(localStorage.getItem(App.data.config.saveKey)).points, S.points()); return r.join(String.fromCharCode(10)); })()" },
  { "eval": "(function(){ const S = App.state; const r = []; const ok = (name, got, exp) => r.push((JSON.stringify(got) === JSON.stringify(exp) ? 'OK  ' : 'NG  ') + name + ' got=' + JSON.stringify(got) + ' exp=' + JSON.stringify(exp)); const json = S.exportJSON(); const obj = JSON.parse(json); ok('export has keys', ['version','player','collection','party','dex','gacha','stats','settings'].every(k => k in obj), true); obj.points = 12345; ok('import', S.importJSON(JSON.stringify(obj)), true); ok('imported points', S.points(), 12345); ok('import garbage', S.importJSON('garbage{'), false); ok('import no player', S.importJSON(JSON.stringify({a:1})), false); ok('points unchanged', S.points(), 12345); const partial = { player: { name: 'ミニ', map: GameData.worldStart.map, x: GameData.worldStart.x, y: GameData.worldStart.y }, points: 7, collection: { zz_test: obj.collection.zz_test, ghost_mon: { speciesId: 'ghost_mon', level: 5 } }, party: ['ghost_mon', 'zz_test'] }; ok('import partial', S.importJSON(partial), true); ok('merged defaults', [S.data.stats.battles, S.data.settings.textSpeed, S.data.freePulls, Array.isArray(S.data.gacha.history)], [0, 'normal', App.data.config.freeGachaPulls, true]); ok('ghost removed', [!!S.data.collection.ghost_mon, S.partyIds()], [false, ['zz_test']]); ok('orphan kept', !!(S.data._orphans && S.data._orphans.ghost_mon), true); ok('warnings', S.loadWarnings.length > 0, true); const bad = { player: { name: 'X', map: 'no_such_map', x: 99, y: 99 }, points: 'abc' }; ok('import bad types', S.importJSON(bad), true); ok('pos reset', [S.data.player.map, S.data.player.x], [GameData.worldStart.map, GameData.worldStart.x]); ok('points type reset', S.data.points, App.data.config.startPoints); return r.join(String.fromCharCode(10)) + String.fromCharCode(10) + '  warnings: ' + S.loadWarnings.join(' / '); })()" },
  { "eval": "(function(){ localStorage.setItem(App.data.config.saveKey, '{broken json'); const ok = App.state.load(); return 'load broken -> ' + ok + ' newgame=' + App.state.isNewGame() + ' backup=' + !!localStorage.getItem(App.data.config.saveKey + '_broken') + ' warn=' + App.state.loadWarnings.length; })()" }
]
