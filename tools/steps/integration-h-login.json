[
  {"goto": "index.html"},
  {"clearStorage": true},
  {"goto": "index.html?autostart=1"},
  {"waitFor": "window.App && App.ready", "timeout": 15000},
  {"eval": "App.gacha.pull('standard',1,{free:true}); App.state.setFlag('gachaGuideDone', true); App.state.data.lastLogin=''; App.state.saveNow(); 'pts=' + App.state.points() + ' days=' + App.state.data.stats.loginDays"},
  {"goto": "index.html"},
  {"waitFor": "window.App && App.booted && document.querySelector('.modal-gift')", "timeout": 15000},
  {"eval": "'tab=' + App.main.currentTab() + ' ready=' + App.ready + ' inputActive=' + App.input.isActive() + ' title=' + App.field.debug.state().title"},
  {"key": "KeyZ"},
  {"key": "ArrowDown", "hold": 500},
  {"wait": 300},
  {"eval": "'after keys: modal=' + !!document.querySelector('.modal-gift') + ' field=' + JSON.stringify(App.field.debug.state()).slice(0,90)"},
  {"shot": "h01-login-bonus"},
  {"click": ".modal-gift .modal-footer button"},
  {"waitFor": "App.ready", "timeout": 5000},
  {"wait": 800},
  {"eval": "'pts=' + App.state.points() + ' topbar=' + document.querySelector('#topbar').innerText.replace(/\\s+/g,' ') + ' days=' + App.state.data.stats.loginDays + ' last=' + App.state.data.lastLogin"},
  {"key": "KeyZ"},
  {"wait": 800},
  {"eval": "var r=['bgm adv=' + App.audio.currentBgm()]; App.main.switchTab('gacha'); r.push('gacha=' + App.audio.currentBgm()); App.main.switchTab('party'); r.push('party=' + App.audio.currentBgm()); App.main.switchTab('dex'); r.push('dex=' + App.audio.currentBgm()); App.main.switchTab('adventure'); r.push('adv=' + App.audio.currentBgm()); App.main.switchTab('gacha'); App.main.switchTab('adventure'); r.push('adv2=' + App.audio.currentBgm()); r.join(' ')"},
  {"click": "#tb-sound"},
  {"wait": 300},
  {"eval": "'sound=' + App.state.setting('sound') + ' muted=' + App.audio.isMuted() + ' btnOff=' + document.querySelector('#tb-sound').classList.contains('is-off')"},
  {"goto": "index.html"},
  {"waitFor": "window.App && App.ready", "timeout": 15000},
  {"eval": "'after reload sound=' + App.state.setting('sound') + ' muted=' + App.audio.isMuted() + ' btnOff=' + document.querySelector('#tb-sound').classList.contains('is-off') + ' modal=' + !!document.querySelector('.modal-gift')"},
  {"click": "#tb-sound"},
  {"wait": 300},
  {"eval": "'sound=' + App.state.setting('sound') + ' muted=' + App.audio.isMuted()"}
]
