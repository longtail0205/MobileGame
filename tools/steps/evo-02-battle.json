[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=adventure" },
  { "waitFor": "window.App && App.ready", "timeout": 15000 },
  { "eval": "new Promise(function(r){var s=document.createElement('script');s.src='tools/battle-test.js';s.onload=function(){r('loaded')};document.body.appendChild(s);})" },
  { "eval": "App.state.addMonster('hinokon', {level: 17}); App.state.addMonster('mizupyon', {level: 8}); var h=App.state.owned('hinokon'); h.exp = App.monster.expForLevel(18,'medium_fast') - 3; h.nickname='ポチ'; window.__mv=function(s){return s.player.moves.findIndex(function(m){return GameData.moves[m.id].power>0})}; window.__before={hp:h.hp, max:App.monster.stats(h).hp, moves:h.moves.map(function(m){return m.id})}; JSON.stringify(window.__before)" },
  { "eval": "window.__r = null; App.battle.startWild('happamaru', 3).then(function(r){ window.__r = r; }); App.battle.isActive()" },
  { "eval": "BattleTest.autoplay({ move: __mv, stopAt: 'evolve' }).then(function(x){ return JSON.stringify(x.stopped) + ' ' + x.log.slice(-3).join(' / '); })", "timeout": 60000 },
  { "wait": 700 },
  { "shot": "b1-evolving" },
  { "eval": "BattleTest.autoplay({ move: __mv, stopAt: 'wait' }).then(function(x){ return App.battle.debugState().text; })", "timeout": 60000 },
  { "wait": 300 },
  { "shot": "b2-evolved" },
  { "eval": "BattleTest.autoplay({ move: __mv }).then(function(x){ return x.log.slice(-6).join(' / '); })", "timeout": 60000 },
  { "waitFor": "window.__r !== null", "timeout": 10000 },
  { "eval": "var h=App.state.owned('hinojishi'); JSON.stringify({ r: window.__r, old: !!App.state.owned('hinokon'), sp: h && h.speciesId, lv: h && h.level, nick: h && h.nickname, hp: h && h.hp, max: h && App.monster.stats(h).hp, party: App.state.partyIds(), moves: h && h.moves.map(function(m){return m.id}), layer: document.getElementById('battle-layer').hidden })" },
  { "eval": "App.state.saveNow(); true" },
  { "goto": "index.html?autostart=1&tab=party" },
  { "waitFor": "window.App && App.ready", "timeout": 15000 },
  { "wait": 500 },
  { "eval": "JSON.stringify({ owned: Object.keys(App.state.data.collection), party: App.state.partyIds(), sel: App.partyTab.selected(), dexOwnedOld: !!App.state.data.dex.owned.hinokon, detail: document.querySelector('.pt-detail').dataset.species, btn: (document.querySelector('.pt-btn-evolve')||{}).textContent })" },
  { "shot": "b3-party-after-reload" }
]
