[
  { "goto": "index.html" },
  { "clearStorage": true },
  { "goto": "index.html?autostart=1&tab=settings" },
  { "waitFor": "window.App && App.ready", "timeout": 8000 },
  { "eval": "JSON.stringify(App.gacha.banners().map(b => b.id)) + ' rates=' + JSON.stringify(App.gacha.rates('standard')) + ' pityRemaining=' + App.gacha.pityRemaining()" },
  { "eval": "(function(){ const out = []; for (const id of ['standard','pickup_sea','pickup_storm']) { const n = 100000; const s = App.gacha.simulate(id, n, { seed: 12345, pity: false, guarantee: false }); const r = App.gacha.rates(id); const bad = []; for (const k of Object.keys(r)) { const p = r[k] / 100; const tol = Math.max(0.05, 4 * Math.sqrt(p * (1 - p) / n) * 100); const diff = s.percent[k] - r[k]; if (Math.abs(diff) > tol) bad.push(k + ':' + s.percent[k].toFixed(3) + ' vs ' + r[k] + ' tol ' + tol.toFixed(3)); } out.push(id + ' ' + (bad.length ? 'NG ' + bad.join(', ') : 'OK') + ' ' + JSON.stringify(Object.fromEntries(Object.entries(s.percent).map(([k, v]) => [k, +v.toFixed(3)])))); } return out.join(' | '); })()" },
  { "eval": "(function(){ const s = App.gacha.simulate('pickup_sea', 100000, { seed: 777, pity: false, guarantee: false }); const out = []; for (const r of ['SR','SSR','UR']) { const ratio = s.pickupByRarity[r] / s.rarity[r]; out.push(r + ' pickupRatio=' + ratio.toFixed(3) + ' (n=' + s.rarity[r] + ') ' + (Math.abs(ratio - 0.5) < 0.05 ? 'OK' : 'NG')); } out.push('aquaroa=' + (s.species.aquaroa / s.pulls * 100).toFixed(3) + '% expect 1.000'); const sr = App.gacha.speciesRates('pickup_sea'); const sum = sr.reduce((a, x) => a + x.rate, 0); out.push('speciesRates sum=' + sum.toFixed(6)); return out.join(' | '); })()" },
  { "eval": "(function(){ const s = App.gacha.simulate('standard', 20000, { seed: 99, mode: 'multi', pity: false }); const d = App.gacha._drawSeries('standard', 10, () => 0, { pity: 0 }, { multi: true, pity: false }); const d2 = App.gacha._drawSeries('standard', 10, (function(){ let i = 0; return () => (i++ === 0 ? 0.9 : 0); })(), { pity: 0 }, { multi: true, pity: false }); return 'sets=' + (s.pulls / 10) + ' withoutGuarantee=' + s.multiWithoutGuarantee + ' forcedGuarantee=' + s.forcedGuarantee + ' (expect ~' + Math.round(Math.pow(0.8, 9) * 2000) + ') | allZero last=' + d[9].rarity + '/' + d[9].forced + ' first9=' + d.slice(0, 9).map(x => x.rarity).join('') + ' | firstSR+ last=' + d2[9].rarity + '/' + d2[9].forced + ' first=' + d2[0].rarity; })()" },
  { "eval": "(function(){ const ps = { pity: 0 }; const d = App.gacha._drawSeries('standard', 100, () => 0, ps, {}); const s = App.gacha.simulate('standard', 300000, { seed: 4242 }); return 'rng0: #100=' + d[99].rarity + '/' + d[99].forced + ' #99=' + d[98].rarity + ' pityAfter=' + ps.pity + ' | sim maxDry=' + s.maxDry + ' forcedPity=' + s.forcedPity + ' SSR+%=' + (s.percent.SSR + s.percent.UR).toFixed(3); })()" },
  { "eval": "(function(){ const G = GameData.gacha; G.banners.push({ id: 't_fire', name: 'テスト', rates: { N: 50, R: 30, SR: 14, SSR: 5, UR: 1 }, pool: App.data.monsters().filter(m => m.types.includes('fire') && m.rarity !== 'UR').map(m => m.id), pickup: [], colors: ['#000','#f00'] }); const r = App.gacha.rates('t_fire'); const sum = Object.values(r).reduce((a, b) => a + b, 0); const pools = App.data.rarityOrder().map(k => k + ':' + App.gacha.pool('t_fire', k).length).join(','); const g = App.gacha.rates('t_fire', 'SR'); G.banners.pop(); return 'pools=' + pools + ' rates=' + JSON.stringify(Object.fromEntries(Object.entries(r).map(([k, v]) => [k, +v.toFixed(3)]))) + ' sum=' + sum.toFixed(6) + ' SR+=' + JSON.stringify(Object.fromEntries(Object.entries(g).map(([k, v]) => [k, +v.toFixed(3)]))); })()" },
  { "eval": "(function(){ const S = App.state; const log = []; let ev = null; App.events.on('gacha:pulled', p => { ev = p; }); log.push('free=' + S.freePulls() + ' pts=' + S.points() + ' can1=' + App.gacha.canPull('standard', 1) + ' canFree=' + App.gacha.canPull('standard', 1, { free: true }) + ' short10=' + App.gacha.shortage('standard', 10)); log.push('pullNoPts=' + App.gacha.pull('standard', 1)); log.push('free10=' + App.gacha.pull('standard', 10, { free: true })); for (let i = 0; i < 3; i++) { const r = App.gacha.pull('standard', 1, { free: true }); log.push('F' + i + ':' + r[0].speciesId + '/' + r[0].rarity + ' new=' + r[0].isNew + ' party=' + r[0].joinedParty); } log.push('free=' + S.freePulls() + ' can=' + App.gacha.canPull('standard', 1, { free: true }) + ' owned=' + S.ownedCount() + ' party=' + S.partyIds().length + ' ev=' + (ev && ev.bannerId + '/' + ev.results.length)); S.addPoints(1000, 'test'); const r10 = App.gacha.pull('pickup_sea', 10); log.push('10連: ' + r10.map(x => x.rarity).join(',') + ' pts=' + S.points() + ' spent=' + S.data.stats.pointsSpent + ' total=' + S.data.gacha.totalPulls + ' hist=' + S.data.gacha.history.length + ' pity=' + S.data.gacha.pity + ' remain=' + App.gacha.pityRemaining()); return log.join(' | '); })()" },
  { "eval": "(function(){ const S = App.state; S.addPoints(100000, 'test'); const id = App.data.monsters()[0].id; const rng = () => 0; S.data.gacha.pity = 0; const out = []; const G = GameData.gacha; G.banners.push({ id: 't_one', name: 'one', rates: { N: 100 }, pool: [id], pickup: [], colors: ['#000','#fff'] }); for (let i = 0; i < 8; i++) { const r = App.gacha.pull('t_one', 1, { rng })[0]; out.push((r.isNew ? 'NEW' : '') + 'LB' + r.limitBreak + (r.refund ? '+' + r.refund : '')); } G.banners.pop(); out.push('pts=' + S.points() + ' lb=' + S.owned(id).limitBreak); return out.join(' '); })()" },
  { "eval": "(function(){ const S = App.state; S.data.gacha.pity = 99; const before = App.gacha.pityRemaining(); const r = App.gacha.pull('standard', 1, { rng: () => 0 })[0]; return 'remainBefore=' + before + ' result=' + r.rarity + '/' + r.forced + ' pityAfter=' + S.data.gacha.pity + ' remain=' + App.gacha.pityRemaining() + ' stats=' + JSON.stringify(App.gacha.stats()); })()" }
]
