# ガチャモン 手動編集 手順書

モンスターの追加・画像の差し替え・ピックアップガチャの作成・マップの作成を、自分で行うための手順です。
項目の細かい意味は [README.md](README.md) の「5. データ編集ガイド」、エディタの画面の説明は「6. データエディタの使い方」にあります。

---

## 目次

0. [はじめに（共通の準備と、変更の反映方法）](#0-はじめに共通の準備と変更の反映方法)
1. [モンスターを追加する](#1-モンスターを追加する)
2. [モンスターの画像を入れる](#2-モンスターの画像を入れる)
3. [進化を追加する](#3-進化を追加する)
4. [ピックアップガチャを作る](#4-ピックアップガチャを作る)
5. [わざを追加する](#5-わざを追加する)
6. [トレーナーを作って配置する](#6-トレーナーを作って配置する)
7. [マップを作る](#7-マップを作る)
8. [確認のチェックリスト](#8-確認のチェックリスト)
9. [困ったとき](#9-困ったとき)

---

## 0. はじめに（共通の準備と、変更の反映方法）

### 0.1 作業の前に必ずバックアップ

`data` フォルダを丸ごとコピーしておきます（例: `data_backup_20260925`）。
うまく動かなくなっても、元のファイルに戻せば直ります。

### 0.2 編集の方法は2通り

| 方法 | 向いていること | 開くもの |
|---|---|---|
| **A. データエディタ** | 入力欄で安全に編集したいとき。参照切れを自動で見つけてくれる | `editor.html`（ダブルクリック） |
| **B. ファイルを直接編集** | たくさんまとめて追加したい、コピー＆貼り付けで作りたいとき | `data/*.js`（VS Code などのテキストエディタ） |

どちらで作業しても、最終的には `data/*.js` の中身が正になります。

### 0.3 エディタで編集したときの反映方法

エディタ上部のボタンで、反映方法を選びます。

1. **「ゲームに一時反映」**（Ctrl+S）
   変更がこのブラウザにだけ保存されます。`index.html` を再読み込みすると反映されます。
   お試し用です。ほかのPCやブラウザには反映されません。
2. **「JSファイルを書き出し」** → ファイルをダウンロード
   できたファイル（例 `monsters.js`）で `data/` フォルダの同じ名前のファイルを置き換えると、ずっと反映されます。
3. ファイルを置き換えたら、**「一時反映を解除」**を押しておきます。
   一時反映が残っていると、ファイルの内容より一時反映のほうが優先されます。

> 保存や書き出しの前に自動で検証が行われ、エラーがあれば一覧で表示されます。エラーが 0 になってから反映してください。

### 0.4 ファイルを直接編集したときの確認方法

1. ファイルを保存します（文字コードは **UTF-8** のまま）。
2. `tools/datacheck.html` をブラウザで開き、エラーが出ないか確認します。
3. `index.html` を開いて（開いていれば再読み込み）ゲームで確かめます。
   データにエラーがあると、ゲーム画面の右下にエラー一覧が出ます。

### 0.5 動作確認を早くするデバッグ機能

`data/config.js` の `debug: true` のとき、ゲームの「その他」タブに次のデバッグボタンがあります。

- `+1000 pt` / `+10000 pt`: ガチャを試すとき
- `全モンスター入手`: 図鑑や編成を確かめるとき
- `パーティ Lv+5`
- `バッジ+1`: 先のマップへ進めるとき
- `リーグリセット`

公開するときは `debug: false` にします。

---

## 1. モンスターを追加する

### A. エディタで追加する

1. `editor.html` を開き、上部の **「モンスター」** タブを選びます。
2. 右上の **「＋ 新規追加」** を押します。空いている図鑑番号と ID が自動で提案されます。
   似たモンスターを元にしたいときは、一覧で選んでから **「複製」** を押します。
3. フォームを埋めます。

| 項目 | 書き方 | 注意 |
|---|---|---|
| ID | 英小文字・数字・`_` のみ（例 `moguron`） | ほかと重複しないこと。**後から変えない**（セーブデータがこのIDを使う） |
| 図鑑番号 No | 整数 | 重複しないこと。図鑑はこの順に並ぶ |
| 名前 | カタカナ推奨（全角5〜6文字くらい） | |
| レア度 | N / R / SR / SSR / UR | ガチャの出やすさがここで決まる |
| タイプ | 1〜2個 | |
| 種族値 | HP・こうげき・ぼうぎょ・とくこう・とくぼう・すばやさ | 合計の目安が画面に出る（N 250〜330 / R 330〜400 / SR 400〜470 / SSR 470〜540 / UR 540〜610） |
| 覚えるわざ | 「行を追加」でレベルとわざを選ぶ | **Lv1 の攻撃わざを必ず1つ以上**入れる（ガチャで入手した直後に戦えるように） |
| 経験値タイプ / 基礎経験値 | 既存の同じレア度のモンスターを参考に | |
| 画像 | 空欄なら自動生成ドット絵 | 手順は「2. 画像を入れる」 |
| ドット絵（体型・色・シード） | 画像が無いときの見た目 | プレビューを見ながら調整。「ランダム」でシードを変えられる |
| ガチャに登場する | チェックを入れるとガチャに出る | 進化後の姿などはチェックを外す |
| 分類・高さ・重さ・説明 | 図鑑に表示される | 説明は、ひらがな多めで文節ごとにスペースを入れるとGBAらしくなる |

4. 「ゲームに一時反映」 → `index.html` を再読み込み → 「その他」タブの「全モンスター入手」または `+1000 pt` でガチャを引いて、表示を確認します。
5. 問題なければ「JSファイルを書き出し」で `monsters.js` を書き出し、`data/monsters.js` と置き換えます（0.3）。

### B. ファイルを直接編集して追加する

1. `data/monsters.js` を開きます。
2. 似たモンスターのブロック（`{` から `},` まで）を丸ごとコピーし、最後の `];` の直前に貼り付けます。
3. `id`・`no`・`name` を、ほかと重ならない値に書き換えます。
4. 残りの項目を書き換えます。書き方の例（実際の ヒノコン のブロックを短くしたもの）:

```js
  {
    id: 'hinokon',
    no: 1,
    name: 'ヒノコン',
    rarity: 'N',
    types: ['fire'],
    baseStats: { hp: 44, atk: 58, def: 40, spa: 52, spd: 46, spe: 62 }, // 合計 302
    learnset: [
      { lv: 1, move: 'tackle' },        // たいあたり
      { lv: 1, move: 'growl' },         // なきごえ
      { lv: 5, move: 'ember' },         // ひのこ
    ],
    expGroup: 'medium_fast',
    baseExp: 62,
    image: '',
    backImage: '',
    sprite: { shape: 'quadruped', colors: ['#f07830', '#f8d880', '#a02818'] },
    gacha: true,
    evolution: { to: 'hinojishi', level: 18 },   // 進化しないなら この行ごと消す
    category: 'ひだねモンスター',
    height: 0.4,
    weight: 5.2,
    desc: 'たてがみの ように はえた ほのおが げんきの しるし。うれしい ときほど おおきく もえあがる。',
  },
```

5. わざの ID は `data/moves.js` のキー（`tackle` など）です。存在しない ID を書くとエラーになります。
6. `tools/datacheck.html` で確認します（0.4）。

> **ガチャへの登場**: `gacha: true` のモンスターは、`pool: null` のバナー（スタンダードガチャなど）に自動で登場します。

---

## 2. モンスターの画像を入れる

### 2.1 画像の用意

| 項目 | おすすめ |
|---|---|
| 形式 | PNG（背景透過） |
| サイズ | 自由（縦横比は保ったまま枠に収まる）。ドット絵なら 64×64 推奨。32 / 48 / 64 / 96 / 192 のように 192 を割り切れるサイズだと、バトル画面（192×192 の枠）でドットの太さが均一になる。正方形でない画像は余白が付く。絵の周りの透明な余白が大きいと小さく見えるので、ギリギリで切り抜く |
| 向き | 正面画像は**左向き〜正面**。バトルでは相手側の表示に使う |
| 背面画像（任意） | 味方側に表示する後ろ姿。無ければ正面画像を左右反転して使う |
| ファイル名 | 半角英数字（例 `hinokon.png`, `hinokon_back.png`）。ID と同じ名前にすると管理しやすい |

### 2.2 手順（ファイルを直接編集する場合）

1. 画像を `assets/monsters/` フォルダに置きます。
2. `data/monsters.js` の該当モンスターに、次のように書きます。

```js
    image: 'assets/monsters/hinokon.png',
    backImage: 'assets/monsters/hinokon_back.png',   // 無ければ '' のまま
```

3. `index.html` を再読み込みして、図鑑・編成・バトルで確認します。

### 2.3 手順（エディタの場合）

1. 「モンスター」タブでモンスターを選び、画像欄の **「ファイル選択…」** を押して画像を選びます。すぐにプレビューが出ます。
2. 保存方法を選びます。
   - **「assets/monsters/ファイル名 のパスを設定」**（おすすめ）: パスだけが書き込まれます。
     **画像ファイル自体を `assets/monsters/` に自分でコピー**してください（ブラウザはフォルダに書き込めないため）。
   - **「データURLとして埋め込む」**: 画像そのものがデータに入ります（コピー不要）。
     ただし一時反映の容量（数MB）を超えると保存できないので、小さな画像だけにしてください。

### 2.4 注意

- パスの大文字・小文字、拡張子（`.png` / `.PNG`）はファイル名と完全に一致させます。
- 画像が見つからないときは自動生成ドット絵で表示されます。「画像が出ずにドット絵のまま」なら、パスの書き間違いを疑ってください。
- 画像を指定すると、`sprite`（自動生成の設定）は使われません。
- トレーナーのバトル用の立ち絵は、`data/trainers.js` の `image` に同じ要領で指定できます。
  マップのタイルは、`data/tiles.js` の記号に `image: 'assets/tiles/xxx.png'`（16×16）を指定できます。

---

## 3. 進化を追加する

1. **進化後のモンスターを、新しいモンスターとして作ります**（手順1）。
   - レア度は進化前と同じにします。
   - `gacha: false` にします（エディタでは「ガチャに登場する」のチェックを外す）。進化後の姿は育成でしか入手できないようにするためです。
   - 覚えるわざには、進化前のわざをすべて入れ、新しいわざを足します。
   - 種族値合計の目安: N の1段目 370〜440 / N の2段目 460〜500 / R の進化 450〜490
2. **進化前のモンスターに、進化先を書きます。**
   - エディタ: 進化前のモンスターの「進化」欄で、進化先とレベルを選びます。
   - ファイル: 進化前のブロックに `evolution: { to: '進化後のid', level: 20 },` を足します。
3. 2段階目も同じ要領で、進化後のモンスターに `evolution` を書きます。分岐（1体から2種類へ進化）はできません。
4. 図鑑で隣に並ぶよう、図鑑番号 `no` を進化前の次にすると見やすくなります（番号を振り直してもセーブデータには影響しません）。
5. 確認: 「その他」タブの「パーティ Lv+5」で進化レベルを超えさせ、「へんせい」タブの「しんかさせる」ボタンで進化できるか見ます。

> 同じ系統は1体として扱います。進化した後に進化前をガチャで引くと、進化後の個体の凸（限界突破）になります。

---

## 4. ピックアップガチャを作る

### A. エディタで作る

1. 「ガチャ」タブを開き、バナー一覧の **「＋ 新規」**（既存のピックアップを元にするなら **「複製」**）を押します。
2. 次の項目を設定します。

| 項目 | 内容 |
|---|---|
| ID | 英小文字・数字・`_`（例 `pickup_forest`）。ほかのバナーと重複しないこと |
| 名前・説明 | ガチャ画面に表示される |
| 提供割合（%） | N / R / SR / SSR / UR の合計を **100** にする（合計がずれるとエラー表示） |
| 対象（pool） | 空（全モンスター）か、出すモンスターをチェックで限定 |
| ピックアップ | 出やすくしたいモンスターにチェック |
| ピックアップ率 | そのレア度が出たとき、ピックアップ対象が選ばれる確率（0.5 = 50%） |
| 背景色 | バナーのグラデーション2色 |

3. 並び順を変えると、ガチャ画面での表示順が変わります（上から順）。
4. 一時反映 → `index.html` で「ガチャ」タブを開き、「提供割合」ボタンで確率を確認します。

### B. ファイルを直接編集して作る

`data/gacha.js` の `banners: [ ... ]` の中に、次のブロックを追加します（例: 実在のバナー「あおき うみの でんせつ」と同じ形）。

```js
    {
      id: 'pickup_sea',
      name: 'あおき うみの でんせつ',
      desc: 'UR アクアロア・SSR シオネ・SR イッカクジラ が でやすい！ UR の しゅつげんりつも 2ばい！',
      rates: { N: 49, R: 30, SR: 14, SSR: 5, UR: 2 },
      pool: null,
      pickup: ['aquaroa', 'shione', 'ikkakujira'],
      pickupRate: 0.5,
      colors: ['#082a5e', '#22c3e0'],
    },
```

- `rates` は合計 100 にします。例のように UR を 1 → 2 に上げたら、ほかをその分下げます。
- `pool: null` は全モンスターが対象です。特定のモンスターだけのガチャにするときは `pool: ['hinokon', 'kamadon', ...]` と書きます。
  pool に含まれないレア度の確率は、自動で他のレア度に振り分けられます。
- `pickup` に書けるのは、ガチャに出るモンスター（`gacha: false` でないもの）です。
- `pickupRate` の例: UR 2%・UR のピックアップ1体・pickupRate 0.5 → そのモンスターは 1% で出ます。
- 10連の「最後の1回 SR 以上確定」と「天井（100回）」は全バナー共通です（`GameData.gacha` の `multiGuarantee` / `pityCount`）。
- ガチャを終了するときは、そのブロックを削除します。ガチャの履歴は残ります。

---

## 5. わざを追加する

1. エディタの「わざ」タブで「＋ 新規追加」を押すか、`data/moves.js` に既存のわざのブロックをコピーして追加します。
2. 主な項目:

| 項目 | 内容 |
|---|---|
| ID | 英小文字など（例 `leafstorm`）。モンスターの learnset はこのIDで書く |
| タイプ / 分類 | 分類は physical（物理）/ special（特殊）/ status（変化） |
| 威力 / 命中 / PP | 変化わざの威力は 0。命中 0 は必中 |
| 追加効果 | 能力変化・状態異常・回復・吸収・反動・ひるみ・連続攻撃（エディタなら種類を選ぶと必要な欄だけ出る） |

3. 追加しただけでは誰も覚えません。モンスターの learnset に追加してください。

---

## 6. トレーナーを作って配置する

### 6.1 トレーナーを作る

1. エディタの「トレーナー」タブで「＋ 新規追加」を押すか、`data/trainers.js` の既存トレーナーのブロックをコピーします。
2. 主な項目:

| 項目 | 内容 |
|---|---|
| ID（キー） | 例 `route5_taro`。マップに置くときにこのIDを使う |
| name / className | 表示は「className の name」（例「わんぱくキッズの ケンタ」） |
| look | フィールドの見た目（肌・髪・髪型・服・ズボン・帽子の色）。エディタなら色を選ぶとプレビューが出る |
| party | 手持ち `{ species: 'モンスターID', level: 12, moves: [...](省略可), limitBreak: 凸(省略可) }` |
| reward | 勝ったときのポイント |
| ai | `'smart'`（相性を考える）/ `'random'` |
| rematch | `'never'`（1回だけ）/ `'daily'`（日付が変わると再戦できる） |
| boss | `true` でボス戦BGM |
| badge | ジムリーダーだけ。`data/badges.js` の id（1つのバッジは1人だけ） |
| intro / lose / win / after | セリフ。配列の1要素が1ページ。`{player}` は主人公の名前に置き換わる |

3. **レベルは、そのエリアに着いたときのレベル上限を超えないように**します（上限: バッジ0個=20 / 1個=30 / 2個=35 / 3個=40 / 4個=50）。
   `data/trainers.js` の先頭コメントに、エリアごとのレベルの目安があります。

### 6.2 マップに配置する

`data/maps.js` の該当マップの `npcs` に追加します（エディタなら「マップ」タブの NPC の JSON 欄）。

```js
      { id: 'tr_route5_taro', x: 10, y: 6, dir: 'left', trainer: 'route5_taro', sight: 4 },
```

- `x, y` は歩けるマスにします（エディタのマップ上にマウスを置くと座標が出ます）。
- `sight` は視線のマス数です。視線に入るとトレーナーが寄ってきます。0 にすると話しかけたときだけ戦います。
- 見た目は、トレーナー側の `look` が使われます。

---

## 7. マップを作る

新しいマップは、**マップ本体を作る → 既存のマップとワープでつなぐ → 中身（野生・NPC・看板）を置く** の順で作ります。

### 7.1 タイル記号（よく使うもの）

| 記号 | 意味 | 記号 | 意味 |
|---|---|---|---|
| `.` | 草地 | `,` | 草むら（野生が出る） |
| `:` | 土の道 | `s` | 砂地 |
| `F` | 花 | `T` | 木（通れない） |
| `W` | 水（通れない） | `B` | 橋 |
| `f` | 柵 | `o` | 岩 |
| `S` | 看板 | `M` | 崖 |
| `h` / `c` / `g` / `j` | 屋根（家 / センター / ガチャショップ / ジム） | `w` / `n` / `D` | 壁 / 窓 / ドア |
| `_` | 床（室内） | `#` | 室内の壁 |
| `=` | カウンター | `K` | じゅうたん |
| `t` / `b` / `p` / `k` | テーブル / 本棚 / 観葉植物 / ベッド | `m` | 出口マット |
| `G` / `H` | ガチャマシン / 回復マシン | `u` | 洞窟の床（野生が出る） |
| `q` | 洞窟の床（出ない） | `r` | 洞窟の壁 |
| `V` | 暗闇 | | |

全記号の一覧は `data/tiles.js` にあります。

### 7.2 マップ本体を作る

**エディタの場合**

1. 「マップ」タブで **「＋ 新規」**（似たマップを元にするなら「複製」）を押し、ID（例 `route5`）と名前、サイズを決めます。
2. 左のタイルパレットで記号を選び、マップ上を **クリック / ドラッグで塗ります**。
   塗りつぶし、スポイト（右クリック）、元に戻す（Ctrl+Z）、サイズ変更が使えます。
3. BGM（`route` / `town` / `cave` など）、`border`（マップの外側に描く記号。屋外は `T`、室内・洞窟は `V` か `r`）、屋内かどうかを設定します。

**ファイルの場合**: `data/maps.js` の `GameData.maps = { ... }` の中に追加します。

```js
  route5: {
    name: '5ばんどうろ',
    bgm: 'route',
    border: 'T',
    indoor: false,
    tiles: [
      'TTTTTT::TTTTTT',
      'T....,,::,,..T',
      'T....,,::,,..T',
      'T.....:::....T',
      'TTTTTT::TTTTTT',
    ],
    encounters: { rate: 0.12, table: [ { species: 'hinokon', min: 20, max: 24, weight: 50 } ] },
    warps: [],
    npcs: [],
    signs: [],
    pickups: [],
  },
```

- **全行を同じ文字数に**します（ずれるとエラー）。左上が `x:0, y:0` です。
- 外周は木 `T` や壁で囲み、出入口にだけ道を開けます。

### 7.3 建物の作り方（見た目がきれいになる形）

| 建物 | 形（上から） |
|---|---|
| 家 | `hhhh` / `hhhh` / `wnDw` |
| センター | `ccccc` / `ccccc` / `wnDnw` |
| ガチャショップ | `ggggg` / `ggggg` / `wnDnw` |
| ジム | `jjjjjj` を3行 / `nwwwwn` / `wwwDww` |

- ドア `D` と窓 `n` は、一番下の壁の行に置きます。
- ドアの前には道 `:` を1マス置きます。建物どうしは1マス以上離します。
- 屋根の紋章は、横幅が奇数のときに中央へきれいに出ます。
- 室内のマップ: 上の壁 `#` を2行にし、出口は `####mm####` のように下の壁にマット `m` を置きます。`border` は `V`、`indoor: true` にします。

### 7.4 ワープでつなぐ（必ず両方向）

ワープは「そのマスに乗った瞬間に、別のマップの指定マスへ移動する」仕組みです。**行きと帰りの両方**を書きます。

```js
// 既存のマップ側（例: town5 の東の端から route5 へ）
      { x: 27, y: 10, to: 'route5', tx: 1, ty: 3, dir: 'right' },
// 新しいマップ側（route5 の西の端から town5 へ戻る）
      { x: 0, y: 3, to: 'town5', tx: 26, ty: 10, dir: 'left' },
```

（座標は説明用の例です。実際のマップに合わせて書いてください。）

- `tx, ty`（移動先）は**歩けるマス**にし、**移動先のワープのマスとは別のマス**にします（同じマスだと往復し続けます）。
- ドア `D` → 室内: 室内の出口マット `m` の**1マス上**に着くようにします。
- 室内のマット `m` → 屋外: ドアの**1マス下**に着くようにします。
- 通行の条件を付けられます:
  - `requireParty: true`: モンスターを持っていないと通れない
  - `requireBadges: 2`: バッジが2個未満だと通れない
  - `requireTrainer: 'トレーナーID'`: そのトレーナーを倒すまで通れない

### 7.5 中身を置く

| 種類 | 書き方の例 | メモ |
|---|---|---|
| 野生 | `encounters: { rate: 0.12, table: [{ species: 'ID', min: 20, max: 24, weight: 50 }] }` | 草むら `,` / 洞窟の床 `u` で出る。weight は出やすさの比率。**レベルはそのエリアの上限以下に** |
| 会話NPC | `{ id: 'girl1', x: 5, y: 4, dir: 'down', look: {...}, move: 'turn', dialog: ['こんにちは！'] }` | move: `still` / `turn` / `wander` |
| 回復係 | 会話NPCに `heal: true` | 回復した場所が全滅時の戻り先になる |
| ガチャ案内 | 会話NPCに `action: 'gacha'` | ガチャタブへ案内する |
| 通せんぼ | 会話NPCに `requireBadges: 3` または `hideAfter: 'トレーナーID'` | 条件を満たすと消える。理由は `dialog` で話させる |
| 看板 | `signs: [{ x: 8, y: 2, text: ['5ばんどうろ', 'きたは カナトコタウン'] }]` | マップ上の `S` の位置に置く |
| 落ちているポイント | `pickups: [{ id: 'route5_p1', x: 3, y: 1, points: 100 }]` | id は全マップで重複しないこと。一度だけ拾える |

- `look` の例: `{ skin: '#f8d0a8', hair: '#503020', hairStyle: 'long', shirt: '#40a040', pants: '#604020', hat: null }`
- セリフの `\n` は改行、配列の1要素が1ページです。

### 7.6 マップの確認

1. `tools/datacheck.html` とエディタの「検証」で、エラーが 0 か確認します（行の長さ、存在しないマップへのワープ、範囲外の座標などを見つけられます）。
2. `index.html` を再読み込みし、実際に歩いて次を確認します。
   - 出入口の両方向のワープ
   - 通れないはずの所を通れないか
   - 閉じ込められる場所がないか
   - NPCに話しかけられるか

---

## 8. 確認のチェックリスト

反映する前に、次を確認してください。

- [ ] `data` フォルダのバックアップを取った
- [ ] `tools/datacheck.html`（またはエディタの「検証」）でエラーが 0
- [ ] ID（モンスター・わざ・トレーナー・マップ・バナー・ポイント）がほかと重複していない
- [ ] 既存の ID を変更・削除していない（する場合は、それを使っているトレーナー・マップ・バナー・進化先も直した）
- [ ] 新しいモンスターの learnset に Lv1 の攻撃わざがある
- [ ] ガチャの rates の合計が 100
- [ ] マップの全行が同じ文字数。ワープは両方向あり、着地点が歩けるマス
- [ ] 野生やトレーナーのレベルが、そのエリアのレベル上限以下
- [ ] エディタで作業した場合、JSファイルを書き出して置き換え、「一時反映を解除」した
- [ ] ゲームで実際に表示・動作を確認した

---

## 9. 困ったとき

| 症状 | 原因と対処 |
|---|---|
| ゲームが真っ白・何も出ない | JS の書き間違い（カンマ `,` 忘れ、括弧の閉じ忘れ、クォート `'` の不一致）の可能性が高い。`tools/datacheck.html` を開くか、ブラウザで F12 → Console の赤いエラーの行番号を見る。直らなければバックアップに戻す |
| 画面右下にエラー一覧が出る | データの参照切れなど。表示された内容（どのIDの何が問題か）を直す |
| 画像が表示されずドット絵のまま | パスのつづり・大文字小文字・拡張子を確認。画像を `assets/monsters/` に置いたか確認 |
| ファイルを直したのに変わらない | エディタの「一時反映」が残っている（ゲームの「その他」タブで解除できる）。またはブラウザの再読み込み（Ctrl+F5）をしていない |
| 新モンスターがガチャに出ない | `gacha: false` になっている、またはバナーの `pool` が限定されている |
| 文字化けする | ファイルを UTF-8 で保存し直す |
| セーブデータがおかしくなった | 「その他」タブの「データリセット」でやり直す（事前に「ファイルに書き出す」でセーブをバックアップできる） |
